# Development Seeds

Future development-only seed scripts belong in this directory. They must never
be treated as production business data. Seed creation follows approved schema
migrations and must preserve the final client decisions and data boundaries.

This directory intentionally contains no seed files in the database foundation
phase.
